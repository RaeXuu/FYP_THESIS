# 神经网络架构图工具

适用于 PyTorch CNN + Coordinate Attention + 残差连接的可视化方案。

---

## 在线工具

| 工具 | 特点 | 地址 |
|------|------|------|
| **NN-SVG** | 专为 CNN 设计，可视化卷积层、池化层，导出 SVG | alexlenail.me/NN-SVG |
| **Netron** | 导入模型文件（.pt/.onnx）自动生成架构图 | netron.app |
| **draw.io** | 通用流程图工具，有神经网络图标库，灵活可定制 | draw.io |
| **PlotNeuralNet** | LaTeX 生成，学术论文风格 | GitHub: HarisIqbal88/PlotNeuralNet |

---

## 代码生成（PyTorch）

### torchviz — 自动追踪计算图

自动展示 Coordinate Attention 内部结构和残差连接（分叉后汇合的箭头）。

```python
import torch
from torchviz import make_dot

model = YourCNNModel()
x = torch.randn(1, 3, 224, 224)  # 根据输入尺寸调整
y = model(x)

dot = make_dot(y, params=dict(model.named_parameters()))
dot.render("model_arch", format="png")
```

> 缺点：图比较底层，节点是操作而非层，适合调试但不适合论文插图。

### 其他

- **torchinfo / torchsummary** — 打印层结构和参数量
- **visualkeras** — Keras 模型彩色分层图（不适用于 PyTorch）

---

## 用途对照

| 用途 | 推荐工具 |
|------|------|
| 快速验证结构正确性 | torchviz |
| 论文 / 报告插图 | draw.io 手动画 |
| 导入已有模型文件 | Netron |
| 学术风格 LaTeX 插图 | PlotNeuralNet |

---

## draw.io 画法建议

针对 CNN + Coordinate Attention + 残差连接：

- 用矩形块表示卷积层、BN、激活函数
- Coordinate Attention 模块单独用不同颜色的子图，内部结构：
  - H/W 方向 Average Pool → Concat → FC → Split → Sigmoid
- 残差连接用**跳跃箭头**绕过主路径后相加
